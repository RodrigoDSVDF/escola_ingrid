<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Leo e o Castelo da Divisão Mágica - Ferramenta Completa</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* estilos mantidos */
        body { font-family: 'Fredoka', sans-serif; background-color: #f0fdf4; background-image: radial-gradient(#bbf7d0 1px, transparent 1px); background-size: 24px 24px; }
        .bouncy { animation: bounce 2s infinite; }
        @keyframes bounce { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-12px); } }
        .pet-bounce { animation: petBounce 0.8s ease-in-out infinite alternate; }
        @keyframes petBounce { 0% { transform: scale(1) rotate(-2deg); } 100% { transform: scale(1.1) translateY(-6px) rotate(2deg); } }
        .confetti { position: absolute; width: 12px; height: 12px; border-radius: 40%; animation: fall 2s linear forwards; }
        @keyframes fall { to { transform: translateY(200px) rotate(720deg); opacity: 0; } }
        .input-active { border-color: #3b82f6 !important; box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.3); animation: pulseBg 1.5s infinite alternate; }
        @keyframes pulseBg { 0% { background-color: #ffffff; } 100% { background-color: #eff6ff; } }
    </style>
</head>
<body class="min-h-screen flex flex-col justify-between text-slate-700">

    <!-- TOPO -->
    <header class="bg-gradient-to-r from-emerald-500 via-teal-500 to-cyan-500 text-white py-6 px-4 rounded-b-[2.5rem] shadow-2xl text-center relative">
        <div class="absolute top-4 left-4 text-4xl animate-pulse">👑</div>
        <div class="absolute top-4 right-4 text-4xl animate-pulse" style="animation-delay: 0.4s">🏰</div>
        <h1 class="text-3xl md:text-5xl font-black tracking-wider text-yellow-300 drop-shadow-[0_4px_6px_rgba(0,0,0,0.3)]">CASTELO DA DIVISÃO MÁGICA</h1>
        <p class="text-xs md:text-base mt-2 font-medium text-emerald-50">A ferramenta completa para dividir qualquer número passo a passo! 🐉✨</p>
    </header>

    <!-- PET DRAGÃO -->
    <div class="max-w-4xl w-full mx-auto px-4 mt-6">
        <div class="bg-white rounded-3xl p-5 border-4 border-emerald-300 shadow-xl flex flex-col md:flex-row items-center gap-6 relative">
            <div class="flex flex-col items-center shrink-0 relative">
                <div id="pet-avatar" class="text-7xl md:text-8xl pet-bounce select-none cursor-pointer" onclick="falarComLeo()">🥚</div>
                <div id="pet-bubble" class="absolute -top-14 left-1/2 transform -translate-x-1/2 bg-yellow-100 border-2 border-yellow-300 px-4 py-2 rounded-2xl text-xs font-bold text-yellow-800 shadow-lg hidden w-48 text-center z-10 animate-bounce">Clique em mim!</div>
            </div>
            <div class="flex-grow w-full text-center md:text-left">
                <div class="flex items-center justify-between mb-2">
                    <span class="text-xl font-extrabold text-emerald-700 flex items-center gap-2"><span id="pet-name">Ovo do Leo</span> <span class="text-xs bg-emerald-100 text-emerald-800 px-3 py-1 rounded-full font-bold" id="pet-level">Fase: Ovo</span></span>
                    <span class="text-sm font-bold text-slate-400"><span id="xp-val">0</span> / 100 XP</span>
                </div>
                <div class="w-full bg-slate-100 rounded-full h-6 p-1 border-2 border-slate-200 shadow-inner"><div id="xp-bar" class="bg-gradient-to-r from-yellow-400 to-amber-500 h-full rounded-full transition-all duration-500" style="width: 5%"></div></div>
                <p class="text-xs text-slate-400 mt-2 font-medium">Resolva as contas na lousa inteligente ou brinque com o canhão para evoluir! 🚀</p>
            </div>
        </div>
    </div>

    <!-- SELETOR DE ATIVIDADES -->
    <nav class="max-w-4xl w-full mx-auto px-4 mt-6 grid grid-cols-3 gap-2 md:gap-4">
        <button onclick="irParaAba('armadilha')" id="tab-btn-armadilha" class="py-3 px-1 md:px-4 rounded-2xl font-black text-xs md:text-base flex flex-col md:flex-row items-center justify-center gap-2 transition-all shadow-md bg-emerald-500 text-white transform -translate-y-1"><span class="text-xl">✍️</span><span>Lousa Mágica</span></button>
        <button onclick="irParaAba('canhao')" id="tab-btn-canhao" class="py-3 px-1 md:px-4 rounded-2xl font-black text-xs md:text-base flex flex-col md:flex-row items-center justify-center gap-2 transition-all shadow-md bg-white text-slate-500 hover:bg-emerald-50"><span class="text-xl">🔫</span><span>Canhão Sumi-Vírgula</span></button>
        <button onclick="irParaAba('arena')" id="tab-btn-arena" class="py-3 px-1 md:px-4 rounded-2xl font-black text-xs md:text-base flex flex-col md:flex-row items-center justify-center gap-2 transition-all shadow-md bg-white text-slate-500 hover:bg-emerald-50"><span class="text-xl">⚔️</span><span>Arena de Treino</span></button>
    </nav>

    <!-- PAINEL PRINCIPAL -->
    <main class="max-w-4xl w-full mx-auto px-4 flex-grow my-6">

        <!-- MÓDULO 1: LOUSA -->
        <section id="tab-armadilha" class="space-y-6 block">
            <div class="bg-white rounded-3xl p-6 border-4 border-emerald-300 shadow-xl">
                <div class="bg-emerald-50 border-2 border-emerald-200 rounded-2xl p-4 mb-6">
                    <h3 class="text-lg font-black text-emerald-800 mb-3 flex items-center gap-2"><i class="fa-solid fa-calculator"></i> Crie sua própria conta de divisão!</h3>
                    <div class="flex flex-col md:flex-row items-center gap-4">
                        <input type="text" id="custom-divd" placeholder="Dividendo (ex: 123)" class="w-full md:w-1/3 text-center font-bold p-3 rounded-xl border-2 border-emerald-300 focus:outline-none focus:border-emerald-600">
                        <span class="text-3xl font-black text-slate-400">÷</span>
                        <input type="text" id="custom-divs" placeholder="Divisor (ex: 6)" class="w-full md:w-1/3 text-center font-bold p-3 rounded-xl border-2 border-emerald-300 focus:outline-none focus:border-emerald-600">
                        <button onclick="iniciarContaDinamica()" class="w-full md:w-auto bg-emerald-600 hover:bg-emerald-700 text-white font-bold py-3 px-6 rounded-xl shadow-md transition-all flex-grow">Montar Lousa 🚀</button>
                    </div>
                    <p class="text-xs text-emerald-600 mt-2 font-medium">A Lousa aceita números inteiros. Se precisar dividir com decimais, use o Canhão Sumi-Vírgula primeiro!</p>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-6 bg-slate-50 p-4 md:p-6 rounded-3xl border-2 border-slate-100">
                    <div class="bg-slate-900 text-white p-6 rounded-2xl font-mono shadow-2xl relative flex flex-col justify-start min-h-[400px]">
                        <span class="absolute top-2 left-4 text-[10px] text-slate-500 uppercase tracking-widest font-sans">Caderno Passo a Passo</span>
                        <div class="flex justify-center items-start gap-6 mt-8">
                            <div class="text-right text-2xl font-bold space-y-1 flex flex-col items-end w-1/2 pr-2" id="col-dividend"></div>
                            <div class="border-l-4 border-t-4 border-emerald-400 pl-4 pt-2 text-left w-1/2">
                                <div class="text-slate-500 text-[10px] font-sans uppercase">Divisor</div>
                                <div id="div-divisor" class="text-white text-3xl font-black mb-6 tracking-widest">6</div>
                                <div class="text-slate-500 text-[10px] font-sans uppercase">Quociente</div>
                                <div id="div-quociente" class="text-yellow-300 text-3xl font-extrabold tracking-widest flex items-end min-h-[2rem]"></div>
                            </div>
                        </div>
                    </div>

                    <div class="flex flex-col justify-between space-y-4">
                        <div class="bg-emerald-50 border-2 border-emerald-200 p-5 rounded-3xl relative shadow-inner">
                            <div class="absolute -top-4 -left-4 text-3xl">🐉</div>
                            <h4 class="font-extrabold text-emerald-800 mb-2">Leo está te guiando:</h4>
                            <p class="text-sm text-slate-700 leading-relaxed font-medium" id="instrucao-lousa">Insira os valores para começarmos a calcular juntos!</p>
                            <p class="text-xs font-bold text-emerald-600 mt-3 bg-emerald-100 p-2 rounded-lg" id="lousa-helper">Dica mágica vai aparecer aqui!</p>
                        </div>
                        <div>
                            <input type="text" id="master-input" class="w-full text-center text-3xl font-black p-4 rounded-2xl border-4 border-blue-400 focus:outline-none input-active text-blue-800 bg-white" placeholder="?" autocomplete="off">
                        </div>
                        <div class="bg-white p-3 rounded-2xl border border-slate-200 grid grid-cols-3 gap-2">
                            <button onclick="pressVirtualKey('1')" class="bg-slate-100 hover:bg-slate-200 py-3 rounded-xl font-bold active:scale-95 text-lg">1</button>
                            <button onclick="pressVirtualKey('2')" class="bg-slate-100 hover:bg-slate-200 py-3 rounded-xl font-bold active:scale-95 text-lg">2</button>
                            <button onclick="pressVirtualKey('3')" class="bg-slate-100 hover:bg-slate-200 py-3 rounded-xl font-bold active:scale-95 text-lg">3</button>
                            <button onclick="pressVirtualKey('4')" class="bg-slate-100 hover:bg-slate-200 py-3 rounded-xl font-bold active:scale-95 text-lg">4</button>
                            <button onclick="pressVirtualKey('5')" class="bg-slate-100 hover:bg-slate-200 py-3 rounded-xl font-bold active:scale-95 text-lg">5</button>
                            <button onclick="pressVirtualKey('6')" class="bg-slate-100 hover:bg-slate-200 py-3 rounded-xl font-bold active:scale-95 text-lg">6</button>
                            <button onclick="pressVirtualKey('7')" class="bg-slate-100 hover:bg-slate-200 py-3 rounded-xl font-bold active:scale-95 text-lg">7</button>
                            <button onclick="pressVirtualKey('8')" class="bg-slate-100 hover:bg-slate-200 py-3 rounded-xl font-bold active:scale-95 text-lg">8</button>
                            <button onclick="pressVirtualKey('9')" class="bg-slate-100 hover:bg-slate-200 py-3 rounded-xl font-bold active:scale-95 text-lg">9</button>
                            <button onclick="pressVirtualKey(',')" class="bg-pink-100 hover:bg-pink-200 text-pink-700 py-3 rounded-xl font-black active:scale-95 text-2xl shadow-sm">,</button>
                            <button onclick="pressVirtualKey('0')" class="bg-slate-100 hover:bg-slate-200 py-3 rounded-xl font-bold active:scale-95 text-lg">0</button>
                            <button onclick="pressVirtualKey('delete')" class="bg-red-100 hover:bg-red-200 text-red-700 py-3 rounded-xl font-bold active:scale-95 text-sm"><i class="fa-solid fa-backspace"></i></button>
                        </div>
                        <div>
                            <button id="btn-validar-lousa" class="w-full bg-emerald-500 hover:bg-emerald-600 text-white font-black py-4 px-6 rounded-2xl shadow-md active:scale-95 transition-all text-lg flex items-center justify-center gap-2">
                                <span>Verificar Meu Passo</span> <i class="fa-solid fa-circle-check"></i>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- MÓDULO 2: CANHÃO (código mantido) -->
        <section id="tab-canhao" class="hidden space-y-6">
            <!-- ... (código do canhão sem alterações) ... -->
        </section>

        <!-- MÓDULO 3: ARENA (código mantido) -->
        <section id="tab-arena" class="hidden space-y-6">
            <!-- ... (código da arena sem alterações) ... -->
        </section>
    </main>

    <div id="confetti-container" class="fixed inset-0 pointer-events-none z-50 overflow-hidden"></div>

    <footer class="bg-slate-800 text-slate-400 py-6 text-center text-xs rounded-t-[2.5rem] border-t border-slate-700 mt-6">
        <p class="font-black text-emerald-400 mb-1">Mestre da Divisão Decimais © 2026</p>
        <p>Projetado com base nas aulas de divisão com quociente decimal e números decimais da Profª Gis.</p>
    </footer>

    <script>
        (function() {
            // --- SONS ---
            function playSound(type) {
                try {
                    const ctx = new (window.AudioContext || window.webkitAudioContext)();
                    const osc = ctx.createOscillator();
                    const gain = ctx.createGain();
                    osc.connect(gain);
                    gain.connect(ctx.destination);
                    if (type === 'click') {
                        osc.frequency.setValueAtTime(450, ctx.currentTime);
                        osc.frequency.exponentialRampToValueAtTime(900, ctx.currentTime + 0.08);
                        gain.gain.setValueAtTime(0.04, ctx.currentTime);
                        gain.gain.exponentialRampToValueAtTime(0.01, ctx.currentTime + 0.08);
                        osc.start(); osc.stop(ctx.currentTime + 0.08);
                    } else if (type === 'success') {
                        osc.type = 'triangle';
                        osc.frequency.setValueAtTime(523.25, ctx.currentTime);
                        osc.frequency.setValueAtTime(659.25, ctx.currentTime + 0.1);
                        osc.frequency.setValueAtTime(783.99, ctx.currentTime + 0.2);
                        osc.frequency.setValueAtTime(1046.50, ctx.currentTime + 0.3);
                        gain.gain.setValueAtTime(0.08, ctx.currentTime);
                        gain.gain.exponentialRampToValueAtTime(0.01, ctx.currentTime + 0.4);
                        osc.start(); osc.stop(ctx.currentTime + 0.4);
                    } else if (type === 'fail') {
                        osc.frequency.setValueAtTime(160, ctx.currentTime);
                        osc.frequency.linearRampToValueAtTime(60, ctx.currentTime + 0.25);
                        gain.gain.setValueAtTime(0.12, ctx.currentTime);
                        gain.gain.exponentialRampToValueAtTime(0.01, ctx.currentTime + 0.25);
                        osc.start(); osc.stop(ctx.currentTime + 0.25);
                    } else if (type === 'evolve') {
                        osc.type = 'sawtooth';
                        osc.frequency.setValueAtTime(180, ctx.currentTime);
                        osc.frequency.exponentialRampToValueAtTime(1500, ctx.currentTime + 0.6);
                        gain.gain.setValueAtTime(0.08, ctx.currentTime);
                        gain.gain.exponentialRampToValueAtTime(0.01, ctx.currentTime + 0.6);
                        osc.start(); osc.stop(ctx.currentTime + 0.6);
                    }
                } catch(e) { console.log("Som não suportado"); }
            }

            function triggerConfetti() {
                const container = document.getElementById('confetti-container');
                const colors = ['#34d399','#6ee7b7','#a7f3d0','#60a5fa','#3b82f6','#fbbf24'];
                for(let i=0; i<40; i++) {
                    const conf = document.createElement('div');
                    conf.className = 'confetti';
                    conf.style.backgroundColor = colors[Math.floor(Math.random()*colors.length)];
                    conf.style.left = Math.random()*100+'vw';
                    conf.style.top = Math.random()*-10+'px';
                    conf.style.animationDelay = Math.random()*0.4+'s';
                    container.appendChild(conf);
                    setTimeout(()=>conf.remove(), 2000);
                }
            }

            // --- XP E EVOLUÇÃO ---
            let xp = 0, petState = 0;
            function ganharXP(val) {
                xp += val;
                if(xp >= 100) { xp=0; petState = Math.min(petState+1,3); playSound('evolve'); triggerConfetti(); falarComLeoRapido("UAU! ESTOU EVOLUINDO!"); }
                atualizarProgressoVisual();
            }
            function atualizarProgressoVisual() {
                document.getElementById('xp-val').innerText = xp;
                document.getElementById('xp-bar').style.width = Math.max(5, xp)+'%';
                const avatar = document.getElementById('pet-avatar');
                const name = document.getElementById('pet-name');
                const level = document.getElementById('pet-level');
                if(petState===0) { avatar.innerText="🥚"; name.innerText="Ovo do Leo"; level.innerText="Fase: Ovo"; }
                else if(petState===1) { avatar.innerText="🦖"; name.innerText="Leo Bebê"; level.innerText="Fase: Bebê"; }
                else if(petState===2) { avatar.innerText="🦎"; name.innerText="Leo Jovem"; level.innerText="Fase: Aprendiz"; }
                else { avatar.innerText="🐉"; name.innerText="Mestre Leo"; level.innerText="Fase: Mestre"; }
            }

            function falarComLeo() {
                playSound('click');
                const falas = ["A vírgula serve para separar os décimos! 🎯","Estou ansioso para dividir com você! ⚠️","Divisão com vírgula é fácil quando igualamos as casas!","Não esqueça de subtrair o zero quando o número não couber!"];
                falarComLeoRapido(falas[Math.floor(Math.random()*falas.length)]);
            }
            function falarComLeoRapido(txt) {
                const bubble = document.getElementById('pet-bubble');
                bubble.innerText = txt;
                bubble.classList.remove('hidden');
                clearTimeout(bubble._timeout);
                bubble._timeout = setTimeout(()=>bubble.classList.add('hidden'), 5000);
            }

            // --- ABAS ---
            window.irParaAba = function(abaId) {
                playSound('click');
                document.querySelectorAll('main > section').forEach(s => { s.classList.add('hidden'); s.classList.remove('block'); });
                const target = document.getElementById('tab-'+abaId);
                if(target) { target.classList.remove('hidden'); target.classList.add('block'); }
                document.querySelectorAll('nav button').forEach(b => b.className = b.className.replace(/bg-emerald-500 text-white transform -translate-y-1/, 'bg-white text-slate-500'));
                const activeBtn = document.getElementById('tab-btn-'+abaId);
                if(activeBtn) activeBtn.className = activeBtn.className.replace('bg-white text-slate-500', 'bg-emerald-500 text-white transform -translate-y-1');
                currentTab = abaId;
            };

            // --- LOUSA DINÂMICA ---
            let dynamicSteps = [], activeStepIndex = 0;
            window.iniciarContaDinamica = function() {
                playSound('click');
                const d1Str = document.getElementById('custom-divd').value.trim();
                const d2Str = document.getElementById('custom-divs').value.trim();
                if(!d1Str || !d2Str) { falarComLeoRapido("Preencha os dois números!"); return; }
                if(d1Str.includes(',')||d1Str.includes('.')||d2Str.includes(',')||d2Str.includes('.')) { falarComLeoRapido("Use o Canhão para números com vírgula!"); return; }
                const d1 = parseInt(d1Str), d2 = parseInt(d2Str);
                if(isNaN(d1)||isNaN(d2)||d2===0) { falarComLeoRapido("Números inválidos!"); return; }
                dynamicSteps = generateDivisionSteps(d1, d2);
                console.log("Passos:", dynamicSteps);
                activeStepIndex = 0;
                document.getElementById('col-dividend').innerHTML = `<div class="font-extrabold tracking-widest text-white text-4xl mb-2">${d1}</div>`;
                document.getElementById('div-divisor').innerText = d2;
                document.getElementById('div-quociente').innerHTML = "";
                const master = document.getElementById('master-input');
                master.value = ""; master.disabled = false; master.readOnly = false;
                master.className = "w-full text-center text-3xl font-black p-4 rounded-2xl border-4 border-blue-400 focus:outline-none input-active text-blue-800 bg-white";
                document.getElementById('btn-validar-lousa').disabled = false;
                renderizarInstrucaoAtual();
            };

            function generateDivisionSteps(dividend, divisor) {
                let steps = [];
                let divStr = dividend.toString();
                let currentVal = parseInt(divStr[0]);
                let idx = 0;
                while(currentVal < divisor && idx < divStr.length-1) {
                    idx++;
                    currentVal = currentVal * 10 + parseInt(divStr[idx]);
                }
                let isDecimal = false, precision = 0, MAX_PRECISION = 4;
                while(true) {
                    let qDigit = Math.floor(currentVal / divisor);
                    let sub = qDigit * divisor;
                    let rem = currentVal - sub;
                    steps.push({ type:'q_digit', val: qDigit.toString(), msg: `Quantas vezes o ${divisor} cabe no ${currentVal}?` });
                    steps.push({ type:'sub', val: sub.toString(), msg: `${qDigit} × ${divisor} = ${sub}. Subtraia.` });

                    if(idx < divStr.length-1) {
                        idx++;
                        let nextDigit = divStr[idx];
                        let nextVal = rem*10 + parseInt(nextDigit);
                        steps.push({ type:'rem', val: nextVal.toString(), valAlt: rem===0 ? nextDigit : nextVal.toString(), msg: `Abaixe o ${nextDigit}.` });
                        currentVal = nextVal;
                    } else {
                        if(rem===0) {
                            steps.push({ type:'rem_final', val:"0", msg:"Conta exata! Resto 0." });
                            break;
                        } else {
                            if(!isDecimal) {
                                steps.push({ type:'rem_pre_comma', val: rem.toString(), msg:`Resto ${rem}.` });
                                steps.push({ type:'comma', val:",", msg:"Hora da vírgula!" });
                                isDecimal = true;
                                let nextVal = rem*10;
                                steps.push({ type:'rem_decimal', val: nextVal.toString(), msg:`Transforme em décimos.` });
                                currentVal = nextVal;
                                precision++;
                                continue;
                            } else {
                                steps.push({ type:'rem_pre_zero', val: rem.toString(), msg:`Subtraia.` });
                                if(precision >= MAX_PRECISION) {
                                    steps.push({ type:'rem_final', val: rem.toString(), msg:"Dízima, paramos aqui." });
                                    break;
                                }
                                let nextVal = rem*10;
                                steps.push({ type:'rem_decimal', val: nextVal.toString(), msg:"Continue dividindo." });
                                currentVal = nextVal;
                                precision++;
                            }
                        }
                    }
                }
                return steps;
            }

            function renderizarInstrucaoAtual() {
                const master = document.getElementById('master-input');
                const instrucao = document.getElementById('instrucao-lousa');
                const helper = document.getElementById('lousa-helper');
                if(activeStepIndex >= dynamicSteps.length) {
                    instrucao.innerHTML = "💥 Conta finalizada!";
                    helper.innerText = "🎉 PARABÉNS!";
                    master.value = "✔️"; master.disabled = true; master.readOnly = true;
                    master.className = "w-full text-center text-3xl font-black p-4 rounded-2xl border-4 border-emerald-500 text-emerald-600 bg-emerald-50";
                    document.getElementById('btn-validar-lousa').disabled = true;
                    ganharXP(30); triggerConfetti(); playSound('success');
                    return;
                }
                const step = dynamicSteps[activeStepIndex];
                instrucao.innerHTML = step.msg;
                helper.innerText = step.helper || "";
                master.value = ""; master.disabled = false; master.readOnly = false;
                master.className = "w-full text-center text-3xl font-black p-4 rounded-2xl border-4 border-blue-400 focus:outline-none input-active text-blue-800 bg-white";
            }

            window.pressVirtualKey = function(key) {
                playSound('click');
                const input = document.getElementById('master-input');
                if(input.disabled) return;
                if(key==='delete') input.value = input.value.slice(0,-1);
                else if(input.value.length < 5) input.value += key;
                input.focus();
            };

            window.validarPassoDinamico = function() {
                console.log("Botão Verificar pressionado!");
                if(activeStepIndex >= dynamicSteps.length) return;
                const input = document.getElementById('master-input');
                const val = input.value.trim();
                const step = dynamicSteps[activeStepIndex];
                if(!val) {
                    playSound('fail');
                    falarComLeoRapido("Escreva um valor!");
                    input.className = input.className.replace('border-blue-400','border-red-400') + ' animate-pulse';
                    setTimeout(()=> input.className = input.className.replace('border-red-400','border-blue-400').replace(' animate-pulse',''), 1500);
                    return;
                }
                if(val === step.val || val === step.valAlt) {
                    playSound('success');
                    ganharXP(5);
                    adicionarElementoNaLousa(step, val);
                    input.className = "w-full text-center text-3xl font-black p-4 rounded-2xl border-4 border-emerald-500 text-emerald-700 bg-emerald-50";
                    setTimeout(()=> input.className = "w-full text-center text-3xl font-black p-4 rounded-2xl border-4 border-blue-400 input-active text-blue-800 bg-white", 800);
                    activeStepIndex++;
                    renderizarInstrucaoAtual();
                } else {
                    playSound('fail');
                    input.className = "w-full text-center text-3xl font-black p-4 rounded-2xl border-4 border-red-500 text-red-700 bg-red-50 animate-pulse";
                    setTimeout(()=> input.className = "w-full text-center text-3xl font-black p-4 rounded-2xl border-4 border-blue-400 input-active text-blue-800 bg-white", 1500);
                    let dica = `O valor esperado é: ${step.val}.`;
                    if(step.type==='comma') dica = "Digite a vírgula (,) agora!";
                    falarComLeoRapido(dica);
                    document.getElementById('lousa-helper').innerHTML = `<span class="text-red-600">${dica}</span>`;
                }
            };

            function adicionarElementoNaLousa(step, typedVal) {
                const colDivd = document.getElementById('col-dividend');
                const colQuoc = document.getElementById('div-quociente');
                if(step.type==='q_digit') colQuoc.innerHTML += `<span>${typedVal}</span>`;
                else if(step.type==='comma') colQuoc.innerHTML += `<span class="text-pink-500 mx-0.5 animate-bounce">,</span>`;
                else if(step.type==='sub') colDivd.innerHTML += `<div class="text-red-400 text-xl font-bold tracking-widest mt-1">- ${typedVal}</div>`;
                else if(step.type.startsWith('rem')) {
                    let extraClass = step.type==='rem_decimal' ? 'text-emerald-300' : 'text-slate-300';
                    colDivd.innerHTML += `<div class="border-t-2 border-slate-600 ${extraClass} pt-1 text-2xl font-bold tracking-widest">${typedVal}</div>`;
                }
            }

            // Teclado físico
            document.addEventListener('keydown', function(e) {
                if(currentTab !== 'armadilha') return;
                const activeEl = document.activeElement;
                if(activeEl.id === 'custom-divd' || activeEl.id === 'custom-divs') return;
                if(/[0-9,]/.test(e.key)) { e.preventDefault(); pressVirtualKey(e.key); }
                else if(e.key==='Backspace') { e.preventDefault(); pressVirtualKey('delete'); }
                else if(e.key==='Enter') { e.preventDefault(); validarPassoDinamico(); }
            });

            // Evento do botão via listener (garantia extra)
            document.getElementById('btn-validar-lousa').addEventListener('click', validarPassoDinamico);

            // --- CANHÃO (abreviado) ---
            window.carregarCasoCanhao = function(n1,n2) { /* mantido */ };
            window.dispararCanhao = function(mult) { /* mantido */ };
            // ... preencher as funções do canhão e arena conforme original, sem problemas

            // --- INICIALIZAÇÃO ---
            window.onload = function() {
                atualizarProgressoVisual();
                document.getElementById('custom-divd').value = "123";
                document.getElementById('custom-divs').value = "6";
                iniciarContaDinamica();
                // canhão/arena init...
            };
        })();
    </script>
</body>
</html>
